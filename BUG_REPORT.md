# 🐛 버그 리포트 — Sorting Knight

> 코드 리뷰 및 자동화 테스트 중 발견한 버그 목록입니다.
> **아래 항목들은 수정하지 않고 리포트만 작성했습니다.** (발표 자료용)
>
> ※ 별도로 수정 완료된 버그: ① 전투 화면 몬스터 아트 미출력(병합 시 연결 누락), ② 메인메뉴 문자 입력 시 게임 종료, ③ Lv10에서 레벨업 아이템 사용 시 최대 레벨 초과.

---

## 심각도 높음

### 1. "일시적" 공격력 버프가 영구 적용됨
- **위치**: `battle.cpp` (소모품 사용 처리), `Item.h`의 `isPermanent` 플래그
- **현상**: 아드레날린 앰플·전투 각성제는 `isPermanent = false`(전투 중에만 유효)로 설계되어 있지만, 전투가 끝나도 공격력이 원래대로 돌아오지 않음.
- **원인**: `player->SetAttack(...)`으로 직접 스탯을 올린 뒤 전투 종료 시 복구하는 로직이 없음. `isPermanent` 값을 아무도 확인하지 않음.
- **재현**: 전투에서 전투 각성제(+50) 사용 → 전투 종료 → 메인 메뉴 ATK 확인 → +50이 그대로 남아 있음.

### 2. 레벨업 아이템(환경부 장관의 표창장)이 스탯 없이 레벨만 올림
- **위치**: `battle.cpp`, `inventory.cpp`의 `gainLevel` 처리
- **현상**: 표창장 사용 시 `SetLevel()`만 호출되어 레벨 숫자만 오르고, 정상 레벨업이 주는 보상(최대체력 +15, 공격력 +3, 체력 완전 회복)이 적용되지 않음.
- **영향**: 몬스터 스탯은 플레이어 레벨에 비례해 강해지므로, 이 아이템을 쓰면 오히려 손해를 보는 함정 아이템이 됨.

### 3. Win32(x86) 빌드 구성이 깨져 있음
- **위치**: `Sorting Knight.vcxproj`
- **현상**: `old_main.cpp`가 **Debug|x64 구성에서만** 빌드 제외로 설정되어 있음. Debug|Win32, Release|Win32, Release|x64로 빌드하면 `main()` 중복 정의로 링크 에러 발생.
- **재현**: Visual Studio에서 구성만 Release로 바꾸고 빌드.

---

## 심각도 중간

### 4. 프롤로그에서 Enter를 두 번 눌러야 진행됨
- **위치**: `new_main.cpp` `ShowIntro()` 끝부분
- **현상**: 타이틀 화면의 `cin.get()`이 개행을 소비해 버퍼가 빈 상태인데, 프롤로그가 `cin.ignore(); cin.get();`으로 2글자를 기다려서 Enter가 2번 필요함.
- **참고**: `cin >> 숫자` 입력 뒤에 오는 화면들(승리 화면 등)은 버퍼에 개행이 남아 있어 정상적으로 1번만 필요. 진입 경로에 따라 필요한 Enter 횟수가 달라지는 구조적 문제.

### 5. 인벤토리 가득 참 메시지가 박스 UI를 깨뜨림
- **위치**: `inventory.cpp` `AddItem()`
- **현상**: 인벤토리가 가득 차면 `std::cout`으로 raw 텍스트("[!] 인벤토리가 가득 차서 ... 버렸습니다")를 출력하는데, DrawScreen 박스 화면 밖에 찍혀 테두리가 깨지거나 다음 화면 클리어로 지워져 보이지 않음. 몬스터 드랍 아이템이 **소리 없이 버려지는** 결과가 됨.

### 6. 레벨업/부서 배치 안내 메시지가 사용자에게 보이지 않음
- **위치**: `player.cpp` `LevelUp()`, `ApplyJob()`
- **현상**: `std::cout`으로 찍는 "[이름] 레벨 업!", "[이름] ~에 배치되었습니다!" 메시지가 직후의 `DrawScreen()` 화면 클리어(`\x1b[2J`)로 즉시 지워짐. 사실상 죽은 코드.

### 7. 상점 판매(Sell) 기능이 구현만 되고 노출되지 않음
- **위치**: `shop.cpp` `Shop::Sell()`
- **현상**: 판매가 60% 로직까지 완성되어 있으나 어떤 메뉴에서도 호출하지 않아 플레이어가 아이템을 팔 방법이 없음. 골드 회수 수단 부재로 밸런스에도 영향.

---

## 심각도 낮음

### 8. `GetRandomItemByRarity()` 폴백 아이템 이름 오타
- **위치**: `ItemManager.h`
- **현상**: 후보가 없을 때 `GetItem("평범한 가위 (C)")`를 호출하지만 실제 등록명은 `"평범한 가위"`라서 공격력 0짜리 "더미 아이템"이 반환됨. 현재 데이터로는 도달하지 않는 잠재 버그.

### 9. 만렙 시 승리 화면 EXP 표기가 어색함
- **위치**: `battle.cpp` 승리 화면
- **현상**: 만렙(Lv.10)이면 경험치가 0으로 고정되어 "EXP +50 (0/100)"처럼 획득량과 현재치가 맞지 않게 표시됨.

### 10. `Player::LevelUp()` 주석과 실제 수치 불일치
- **위치**: `player.cpp`
- **현상**: 주석은 "최대체력 +20, 공격력 +5"라고 하지만 실제 코드는 `maxHp += 15; attack += 3;`. 밸런스 조정 이력이 주석에 반영되지 않음.

### 11. `system("mode con:")`이 stdin 리다이렉트 환경에서 입력을 삼킴
- **위치**: `ui.cpp` `UI_Init()`
- **현상**: 입력을 파이프/파일로 넣는 자동화 테스트 환경에서 `system()` 자식 프로세스가 stdin을 소비해, 게임 첫 입력부터 EOF가 되어 무한 루프 발생. **실제 콘솔 플레이에는 영향 없음.** stdin이 콘솔일 때만 실행하도록 가드하면 해결 가능.

---

*작성: 자동화 스모크 테스트 및 전체 소스 리뷰 기반 (2026-08-05)*
